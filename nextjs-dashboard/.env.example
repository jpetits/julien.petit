# Database (Neon / Vercel Postgres)
# Required — used by the dashboard and all DB queries
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Auth
# Required in production — generate with: openssl rand -base64 32
AUTH_SECRET=

# x402 payment proxy (optional — only needed for /protected and /api/weather routes)
FACILITATOR_URL=
RESOURCE_EVM_ADDRESS=

# x402 paywall UI (optional — defaults shown)
APP_NAME=Next x402 Demo
APP_LOGO=/x402-icon-blue.png
